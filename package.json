{
  "name": "@belelabestia/tstd",
  "version": "0.1.1",
  "type": "module",
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "start": "tsc -p tsconfig.build.json --watch",
    "test": "tsc --noEmit && tsx --test src/**/*.spec.ts",
    "build": "tsc -p tsconfig.build.json && node -e \"import('./dist/index.js')\""
  },
  "devDependencies": {
    "@types/node": "^24.2.0",
    "tsx": "^4.20.3",
    "typescript": "^5.9.2"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  }
}
